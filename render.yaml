services:
  - type: web
    name: sdss-backend
    env: node
    region: singapore
    plan: free
    branch: main
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: PORT
        value: 4000
      - key: JWT_SECRET
        value: supersecret
